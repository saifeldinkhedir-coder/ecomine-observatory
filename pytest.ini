[pytest]
# Put the repo root on sys.path so `pytest` works from a clean checkout without
# the caller having to set PYTHONPATH. Without this, a bare `pytest` fails to
# import ecomine_core (only `python -m pytest` happens to work, because that form
# adds the current directory to sys.path).
pythonpath = .
testpaths = tests
