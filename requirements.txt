# EcoMine Observatory — runtime dependencies
# Pinned as lower bounds; run `pip freeze > requirements.lock.txt` once your
# environment works, and commit that alongside for exact reproducibility.

earthengine-api>=1.4.0
geemap>=0.35.0
leafmap>=0.39.0
geopandas>=1.0.0
rasterio>=1.3.0

# Notebook (Stage 1 visual check)
jupyterlab>=4.2.0
ipywidgets>=8.1.0

# Interface (Stage 5)
streamlit>=1.40.0

# PDF reports (report.py). Pure-Python, no system libraries needed — chosen
# over weasyprint/wkhtmltopdf precisely so `pip install -r requirements.txt`
# is enough on Windows, macOS and Linux alike.
reportlab>=4.0.0

# Tests — ecomine_core, provenance and monitoring need nothing but the
# standard library, which is why they can run in CI without credentials
pytest>=8.0.0
# reads generated PDFs back in tests/test_report.py
pypdf>=4.0.0
